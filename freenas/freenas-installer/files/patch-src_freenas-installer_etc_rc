--- src/freenas-installer/etc/rc.orig	2019-05-03 16:23:05 UTC
+++ src/freenas-installer/etc/rc
@@ -56,7 +56,7 @@ if [ ${dlv:=0} -ne 0 -o -f /etc/diskless ]; then
 fi
 	
 while :; do
-    /etc/install.sh
+    /etc/installer12.sh
 #    TERM=xterm \
 #       PYTHONPATH=/usr/local/lib \
 #       PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/sbin:/usr/local/bin:/rescue \
